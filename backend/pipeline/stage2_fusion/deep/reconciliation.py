"""Sub-stage 2.3.4: three passes — split, enclosure, scoring."""

from __future__ import annotations

from typing import Any


def reconcile(fused_rooms: Any, fused_boundaries: Any) -> Any:
    raise NotImplementedError("Split across walls, enclosure checks, global scoring.")
